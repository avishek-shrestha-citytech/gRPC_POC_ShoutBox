<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Night theme also forced to LIGHT — we control all colors explicitly. -->
    <style name="Base.Theme.GRPC_POC_ShoutBox" parent="Theme.Material3.Light.NoActionBar">
        <!-- No dark-specific customizations needed — light is used everywhere. -->
    </style>
</resources>
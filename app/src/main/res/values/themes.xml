<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme — forced to LIGHT so dark mode doesn't affect colors. -->
    <style name="Base.Theme.GRPC_POC_ShoutBox" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.GRPC_POC_ShoutBox" parent="Base.Theme.GRPC_POC_ShoutBox" />
</resources>